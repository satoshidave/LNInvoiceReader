import React from 'react';
import { Container, MainContainer, Text } from '../../components';

const Disconnected = () => (
    <MainContainer>
        <Container alignItems='center' justifyContent='center'>
            <Text text='disconnected_description' />
        </Container>
    </MainContainer>
);

export default Disconnected;
